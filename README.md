## Dawa cache prototype

The project is a micronaut project with using Ehcache.

Do the following to run tests:

* Install Java 11 or higher
* run `./gradlew test` on mac linux and `gradlew.bat test` on windows



